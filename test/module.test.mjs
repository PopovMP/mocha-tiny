'use strict'

import {ok} from 'assert'
import {describe, it} from '../index.js'

describe('Test module', () => {

	describe('Run a test from a module', () => {

		it('it is true', () => {
			ok(true)
		})
	})
})
